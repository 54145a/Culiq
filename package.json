{
	"name": "curio",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"description": "Curio — browser agent Chrome extension",
	"scripts": {
		"dev": "TARGET=chrome vite",
		"dev:firefox": "TARGET=firefox vite",
		"build": "npm run build:chrome && npm run build:firefox",
		"build:chrome": "TARGET=chrome vite build",
		"build:firefox": "TARGET=firefox vite build",
		"check": "tsc --noEmit"
	},
	"devDependencies": {
		"@crxjs/vite-plugin": "^2.0.0-beta.34",
		"@types/chrome": "^0.0.291",
		"typescript": "^5.7.2",
		"vite": "^6.0.7",
		"web-ext": "^10.1.0"
	},
	"dependencies": {
		"marked": "^18.0.3"
	}
}
